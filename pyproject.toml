[project]
name = "ds-agent"
version = "0.1.0"
description = "Comprehensive coding agent powered by DeepSeek"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
    "pyyaml>=6.0",
]
